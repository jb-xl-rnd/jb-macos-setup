#!/bin/bash
# Quick wrapper for LLM client
# Generated by Ansible - Do not edit directly

VENV_PATH="{{ ansible_env.HOME }}/llm-workspace/llm-venv"
CLIENT_PATH="{{ ansible_env.HOME }}/.local/bin/llm-client"

# Check if virtual environment exists
if [ ! -d "$VENV_PATH" ]; then
    echo "Setting up Python environment..."
    {{ ansible_env.HOME }}/.local/bin/uv venv "$VENV_PATH"
    source "$VENV_PATH/bin/activate"
    {{ ansible_env.HOME }}/.local/bin/uv pip install requests
else
    source "$VENV_PATH/bin/activate" 2>/dev/null
fi

# Run the client
exec python "$CLIENT_PATH" "$@"
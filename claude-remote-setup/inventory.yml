---
all:
  children:
    server:
      hosts:
        remote_mac:
          ansible_host: jbs-mac-mini.tailde191e.ts.net
          ansible_user: "{{ target_user }}"
          ansible_ssh_private_key_file: ~/.ssh/jb-remote-setup_ed25519
          ansible_ssh_extra_args: "-o IdentitiesOnly=yes"
    client:
      hosts:
        localhost:
          ansible_connection: local

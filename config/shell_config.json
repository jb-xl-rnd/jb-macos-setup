{
  "zsh_additions": [
    {
      "name": "pyenv_config",
      "description": "Configure Python environment with pyenv",
      "content": "export PYENV_ROOT=\"$HOME/.pyenv\"\nexport PATH=\"$PYENV_ROOT/bin:$PATH\"\neval \"$(pyenv init --path)\"\neval \"$(pyenv init -)\""
    },
    {
      "name": "uv_config",
      "description": "Configure UV as a PIP replacement",
      "content": "# Setup UV as the preferred Python package installer\nexport UV_VIRTUALENV_ROOT=\"$HOME/.venvs\"\nalias pip=\"uv pip\"\nalias pipi=\"uv pip install\"\nalias venv=\"uv venv\"\n\n# Create a uvm function to create and manage virtualenvs more easily\nuvm() {\n  local cmd=\"$1\"\n  shift\n  case \"$cmd\" in\n    create)\n      local name=\"$1\"\n      uv venv \"$UV_VIRTUALENV_ROOT/$name\"\n      ;;\n    activate)\n      local name=\"$1\"\n      source \"$UV_VIRTUALENV_ROOT/$name/bin/activate\"\n      ;;\n    list)\n      ls -la \"$UV_VIRTUALENV_ROOT\"\n      ;;\n    *)\n      echo \"Usage: uvm [create|activate|list] [name]\"\n      ;;\n  esac\n}"
    },
    {
      "name": "custom_prompt",
      "description": "Custom prompt showing username, directory, and subdirectory",
      "content": "set_prompt() {\n  USER_NAME=\"%F{red}$(whoami)%f\"\n  BASEDIR=${PWD##*/}\n  SUBDIR=${PWD%/*}\n  SUBDIR=${SUBDIR##*/}\n  PS1=\"$USER_NAME@%F{green}$SUBDIR/%F{blue}$BASEDIR %f\\$ \"\n}\nprecmd_functions+=(set_prompt)"
    },
    {
      "name": "neofetch_alias",
      "description": "Alias neofetch to macchina",
      "content": "alias neofetch='echo \"neofetch is not installed, using macchina instead\"; macchina'"
    },
    {
      "name": "lsblk_alias",
      "description": "Alias lsblk to macOS diskutil list",
      "content": "alias lsblk='echo \"lsblk is not available on macOS, using diskutil list instead\"; diskutil list'"
    },
    {
      "name": "kitty_pane_info",
      "description": "Show pane info and uptime in Kitty terminal splits",
      "content": "# Kitty pane identification and uptime display\nif [[ \"$TERM\" == \"xterm-kitty\" ]]; then\n  # Function to get current window/pane info\n  get_kitty_pane_info() {\n    local window_id=\"${KITTY_WINDOW_ID:-unknown}\"\n    local pane_num=\"${KITTY_LISTEN_ON##*/}\"\n    local uptime_info=$(uptime | sed 's/.*up //' | sed 's/,.*//')\n    echo \"[Pane: ${pane_num: -1} | Up: $uptime_info]\"\n  }\n  \n  # Enhanced prompt for Kitty with pane info\n  kitty_set_prompt() {\n    local pane_info=\"%F{yellow}$(get_kitty_pane_info)%f\"\n    USER_NAME=\"%F{red}$(whoami)%f\"\n    BASEDIR=${PWD##*/}\n    SUBDIR=${PWD%/*}\n    SUBDIR=${SUBDIR##*/}\n    PS1=\"$pane_info\\n$USER_NAME@%F{green}$SUBDIR/%F{blue}$BASEDIR %f\\$ \"\n  }\n  \n  # Show pane info on terminal start\n  echo \"\\n$(get_kitty_pane_info)\"\n  echo \"Terminal started at $(date '+%H:%M:%S')\"\n  \n  # Override the default prompt function for Kitty\n  precmd_functions=(kitty_set_prompt)\nfi"
    }
  ]
}